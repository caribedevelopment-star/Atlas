import{CalendarDays,MapPinned,Route}from'lucide-react';import Link from'next/link';import type{MapTrip}from'@/types/map';
export function TripPopup({trip}:{trip:MapTrip}){return <article className="w-64 bg-zinc-950 p-3 text-zinc-100"><span className="text-[10px] font-semibold uppercase tracking-wider text-sky-300">Viaje</span><h3 className="mt-1 text-base font-semibold">{trip.title}</h3><div className="mt-3 space-y-2 text-xs text-zinc-500"><p className="flex gap-1.5"><MapPinned className="h-3.5 w-3.5"/>{trip.stops.length} paradas</p><p className="flex gap-1.5"><Route className="h-3.5 w-3.5"/>{trip.points.length} puntos de ruta</p>{trip.year&&<p className="flex gap-1.5"><CalendarDays className="h-3.5 w-3.5"/>{trip.year}</p>}</div>{trip.stops.length>0&&<ol className="mt-3 border-l border-white/10 pl-3">{trip.stops.slice(0,5).map((stop)=><li key={stop.id} className="py-1 text-xs text-zinc-400">{stop.title}</li>)}</ol>}<Link href={`/trips/${trip.id}`} className="mt-3 block rounded-full bg-white px-3 py-2 text-center text-xs font-semibold text-zinc-950">Ver viaje</Link></article>}
