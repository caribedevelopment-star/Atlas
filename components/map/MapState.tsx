import { AlertCircle, Loader2, MapPinned, RefreshCw } from 'lucide-react';
export function MapLoadingState() { return <div className="flex h-full items-center justify-center bg-zinc-950" role="status"><Loader2 className="mr-2 h-5 w-5 animate-spin text-rose-400" /><span className="text-sm text-zinc-400">Cargando mapa Atlas…</span></div>; }
export function MapErrorState({ message, retry }: { message: string; retry: () => void }) { return <div className="flex h-full items-center justify-center bg-zinc-950 p-6"><div role="alert" className="max-w-sm text-center"><AlertCircle className="mx-auto h-8 w-8 text-red-400" /><h2 className="mt-3 font-semibold text-white">No se pudo cargar el mapa</h2><p className="mt-2 text-sm text-zinc-500">{message}</p><button onClick={retry} className="mt-5 inline-flex items-center gap-2 rounded-full bg-white px-4 py-2 text-sm font-semibold text-zinc-950"><RefreshCw className="h-4 w-4" />Reintentar</button></div></div>; }
export function MapEmptyState({ reset }: { reset: () => void }) { return <div className="pointer-events-none absolute inset-x-4 bottom-28 z-[800] sm:bottom-6 flex justify-center"><div role="status" className="pointer-events-auto max-w-sm rounded-2xl border border-white/10 bg-zinc-950/95 p-4 text-center shadow-2xl backdrop-blur"><MapPinned className="mx-auto h-6 w-6 text-zinc-500" /><p className="mt-2 text-sm font-medium text-white">No hay ubicaciones visibles</p><p className="mt-1 text-xs text-zinc-500">Solo se muestran registros dinámicos con coordenadas válidas.</p><button onClick={reset} className="mt-3 text-xs text-rose-300">Restablecer filtros</button></div></div>; }
