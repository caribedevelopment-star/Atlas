import Image from'next/image';import{CalendarDays,Edit3,Trash2}from'lucide-react';import Link from'next/link';import type{AtlasTrip}from'@/types/trip';
export function TripHeader({trip,owner,onDelete}:{trip:AtlasTrip;owner:boolean;onDelete:()=>void}){return <header className="overflow-hidden rounded-[2rem] border border-white/10 bg-zinc-900/60"><div className="relative h-56 bg-zinc-900 sm:h-72">{trip.coverImageUrl&&<Image src={trip.coverImageUrl} alt={`Portada de ${trip.title}`} fill unoptimized={trip.coverImageUrl.startsWith('http')} className="object-cover"/>}<div className="absolute inset-0 bg-gradient-to-t from-zinc-950 via-zinc-950/30 to-transparent"/><div className="absolute inset-x-0 bottom-0 p-6 sm:p-8"><p className="text-xs uppercase tracking-widest text-rose-300">{trip.visibility}</p><h1 className="mt-2 text-3xl font-semibold text-white sm:text-5xl">{trip.title}</h1><p className="mt-3 flex items-center gap-2 text-sm text-zinc-300"><CalendarDays className="h-4 w-4"/>{trip.startDate} — {trip.endDate}</p></div></div>{trip.description&&<p className="p-6 leading-7 text-zinc-300 sm:p-8">{trip.description}</p>}{owner&&<div className="flex gap-2 border-t border-white/10 p-4"><Link href={`/trips/${trip.id}/edit`} className="flex items-center gap-2 rounded-full bg-white px-4 py-2 text-sm font-semibold text-zinc-950"><Edit3 className="h-4 w-4"/>Editar</Link><button onClick={onDelete} className="flex items-center gap-2 rounded-full border border-red-500/20 px-4 py-2 text-sm text-red-300"><Trash2 className="h-4 w-4"/>Eliminar</button></div>}</header>}
