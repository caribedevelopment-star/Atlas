'use client';
import { Search, SlidersHorizontal, X } from 'lucide-react'; import { useState } from 'react'; import type { AtlasMapFilters, AtlasMapSnapshot, MapLayer, MapSource } from '@/types/map'; import { MapLayerSelector } from './MapLayerSelector';
export function MapToolbar({ filters, snapshot, count, setQuery, setYear, setParticipant, toggleLayer, toggleSource }: { filters: AtlasMapFilters; snapshot: AtlasMapSnapshot; count: number; setQuery: (value: string) => void; setYear: (value: string) => void; setParticipant: (value: string) => void; toggleLayer: (value: MapLayer) => void; toggleSource: (value: MapSource) => void }) { const [open,setOpen] = useState(false); return <div className="absolute inset-x-3 top-3 z-[900] sm:inset-x-5 sm:top-5"><div className="mx-auto flex max-w-5xl items-center gap-2"><label className="relative min-w-0 flex-1"><span className="sr-only">Buscar en el mapa</span><Search className="absolute left-3.5 top-1/2 h-4 w-4 -translate-y-1/2 text-zinc-500" /><input type="search" value={filters.query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar lugar, vino o memoria" className="h-11 w-full rounded-2xl border border-white/10 bg-zinc-950/90 pl-10 pr-3 text-sm text-white shadow-2xl backdrop-blur-xl outline-none focus:border-rose-400" /></label><button onClick={() => setOpen(true)} aria-expanded={open} className="relative flex h-11 items-center gap-2 rounded-2xl border border-white/10 bg-zinc-950/90 px-4 text-sm text-white shadow-2xl backdrop-blur-xl focus:outline-none focus-visible:ring-2 focus-visible:ring-rose-400"><SlidersHorizontal className="h-4 w-4" /><span className="hidden sm:inline">Capas</span><span className="rounded-full bg-rose-500 px-1.5 text-[10px]">{count}</span></button></div>{open && <div className="fixed inset-0 z-[1000] bg-black/60" onMouseDown={(event) => event.target === event.currentTarget && setOpen(false)}><section role="dialog" aria-modal="true" aria-labelledby="map-filters-title" className="absolute inset-x-0 bottom-0 max-h-[85dvh] overflow-y-auto rounded-t-[2rem] border border-white/10 bg-zinc-950 p-5 sm:inset-y-0 sm:left-auto sm:w-96 sm:rounded-none"><div className="mb-5 flex items-center justify-between"><h2 id="map-filters-title" className="font-semibold text-white">Mapa y filtros</h2><button onClick={() => setOpen(false)} aria-label="Cerrar filtros" className="rounded-full p-2 text-zinc-400 hover:bg-white/10"><X className="h-5 w-5" /></button></div><MapLayerSelector filters={filters} toggleLayer={toggleLayer} toggleSource={toggleSource} /><div className="mt-5 grid gap-4"><Select label="Año" value={filters.year} onChange={setYear} options={snapshot.years.map((value) => ({ value, label: value }))} /><Select label="Participante" value={filters.participant} onChange={setParticipant} options={snapshot.participants.map((person) => ({ value: person.id, label: person.name }))} /></div></section></div>}</div>; }
function Select({ label, value, onChange, options }: { label: string; value: string; onChange: (value: string) => void; options: Array<{ value: string; label: string }> }) { return <label className="text-xs text-zinc-500">{label}<select value={value} onChange={(event) => onChange(event.target.value)} className="mt-1 h-11 w-full rounded-xl border border-white/10 bg-zinc-900 px-3 text-sm text-white"><option value="">Todos</option>{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>; }
