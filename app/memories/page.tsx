'use client';
import Link from 'next/link'; import { useMemo, useState } from 'react'; import { CalendarDays, Loader2, MapPin, Plus, RefreshCw, Route } from 'lucide-react'; import { useMemoryArchive } from '@/hooks/use-memory-archive';
export default function MemoriesPage(){const archive=useMemoryArchive(),[query,setQuery]=useState(''),[tab,setTab]=useState<'all'|'memories'|'trips'>('all');const memories=useMemo(()=>archive.memories.filter((item)=>`${item.title} ${item.description??''} ${item.place??''}`.toLocaleLowerCase('es').includes(query.toLocaleLowerCase('es'))),[archive.memories,query]);const trips=useMemo(()=>archive.trips.filter((item)=>`${item.title} ${item.description??''} ${item.cities.join(' ')}`.toLocaleLowerCase('es').includes(query.toLocaleLowerCase('es'))),[archive.trips,query]);return <main className="min-h-screen bg-zinc-950 px-4 py-6 text-zinc-100 sm:px-6 sm:py-8"><div className="mx-auto max-w-7xl"><header className="flex flex-col gap-6 rounded-[2rem] border border-white/10 bg-[radial-gradient(circle_at_85%_0%,rgba(244,63,94,.16),transparent_36%),linear-gradient(145deg,#27272a,#09090b)] p-6 sm:flex-row sm:items-end sm:justify-between sm:p-8"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-rose-300">Archivo personal</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.045em]">Memorias y viajes</h1><p className="mt-2 text-sm text-zinc-400">Todo lo que guardes aparecerá aquí y, si tiene ubicación, también en el mapa.</p></div><Link href="/memories/new" className="inline-flex h-11 items-center justify-center gap-2 rounded-full bg-white px-5 text-sm font-semibold text-zinc-950"><Plus className="h-4 w-4"/>Añadir</Link></header><div className="mt-6 flex flex-col gap-3 rounded-2xl border border-white/10 bg-zinc-900/50 p-3 sm:flex-row"><input value={query} onChange={(event)=>setQuery(event.target.value)} placeholder="Buscar en tu archivo" className="h-11 min-w-0 flex-1 rounded-xl border border-white/10 bg-zinc-950 px-4 text-sm outline-none focus:border-rose-400"/><div className="grid grid-cols-3 gap-1 rounded-xl bg-zinc-950 p-1">{([['all','Todo'],['memories','Memorias'],['trips','Viajes']] as const).map(([key,label])=><button key={key} onClick={()=>setTab(key)} className={`rounded-lg px-3 py-2 text-xs ${tab===key?'bg-white text-zinc-950':'text-zinc-500'}`}>{label}</button>)}</div></div>{archive.loading?<div className="flex min-h-64 items-center justify-center gap-2 text-sm text-zinc-500"><Loader2 className="h-4 w-4 animate-spin"/>Cargando archivo…</div>:archive.error?<div role="alert" className="mt-8 rounded-2xl border border-red-500/20 bg-red-500/5 p-6 text-center"><p className="text-sm text-red-200">{archive.error}</p><button onClick={()=>void archive.refresh()} className="mt-4 inline-flex items-center gap-2 text-xs text-white"><RefreshCw className="h-3.5 w-3.5"/>Reintentar</button></div>:<div className="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{tab!=='trips'&&memories.map((memory)=><article key={memory.id} className="rounded-2xl border border-white/10 bg-zinc-900/50 p-5"><div className="flex items-center justify-between"><span className="flex items-center gap-1.5 text-xs text-rose-300"><MapPin className="h-3.5 w-3.5"/>Memoria</span>{memory.date&&<time className="text-xs text-zinc-600">{format(memory.date)}</time>}</div><h2 className="mt-4 text-lg font-semibold">{memory.title}</h2>{memory.place&&<p className="mt-1 text-xs text-zinc-500">{memory.place}</p>}{memory.description&&<p className="mt-3 line-clamp-3 text-sm leading-6 text-zinc-400">{memory.description}</p>}</article>)}{tab!=='memories'&&trips.map((trip)=><article key={trip.id} className="rounded-2xl border border-sky-500/15 bg-sky-500/[.04] p-5"><div className="flex items-center justify-between"><span className="flex items-center gap-1.5 text-xs text-sky-300"><Route className="h-3.5 w-3.5"/>Viaje</span><span className="text-xs text-zinc-600">{trip.stops.length} paradas</span></div><h2 className="mt-4 text-lg font-semibold">{trip.title}</h2><p className="mt-2 flex items-center gap-1.5 text-xs text-zinc-500"><CalendarDays className="h-3.5 w-3.5"/>{format(trip.startDate)} — {format(trip.endDate)}</p>{trip.cities.length>0&&<p className="mt-3 text-sm text-zinc-400">{trip.cities.join(' · ')}</p>}</article>)}{((tab!=='trips'&&memories.length===0)&&(tab!=='memories'&&trips.length===0))&&<div className="col-span-full rounded-2xl border border-dashed border-white/10 p-12 text-center text-sm text-zinc-500">No hay resultados en tu archivo.</div>}</div>}</div></main>}
function format(value:string){const date=new Date(value);return Number.isNaN(date.getTime())?value:new Intl.DateTimeFormat('es',{dateStyle:'medium'}).format(date)}
