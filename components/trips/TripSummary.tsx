import {Camera,Globe2,MapPin,Users,Wine}from'lucide-react';import type{AtlasTrip}from'@/types/trip';
export function TripSummary({trip}:{trip:AtlasTrip}){const data=[[MapPin,trip.stops.length,'Memorias/paradas'],[Wine,trip.wines.length,'Vinos'],[Camera,trip.photos.length,'Fotos'],[Users,trip.participants.length,'Participantes'],[Globe2,trip.countries.length,'Países']]as const;return <dl className="grid grid-cols-2 gap-3 sm:grid-cols-5">{data.map(([Icon,value,label])=><div key={label} className="rounded-2xl border border-white/10 bg-zinc-900/60 p-4"><Icon className="h-4 w-4 text-rose-300"/><dd className="mt-2 text-2xl font-semibold text-white">{value}</dd><dt className="text-xs text-zinc-500">{label}</dt></div>)}</dl>}
