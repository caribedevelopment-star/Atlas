'use client';
import Image from 'next/image'; import { useEffect, useState } from 'react'; import { Wine } from 'lucide-react'; import { cx } from './utils';
export interface WineBottleImageProps { src?: string|null; alt:string; priority?:boolean; className?:string; imageClassName?:string; variant?:'contain'|'cover' }
export function WineBottleImage({src,alt,priority=false,className,imageClassName,variant='contain'}:WineBottleImageProps){const[failed,setFailed]=useState(false);useEffect(()=>setFailed(false),[src]);const visible=Boolean(src&&!failed);return <div className={cx('relative isolate overflow-hidden rounded-[1.75rem] border border-white/10 bg-[radial-gradient(circle_at_50%_0%,rgba(255,255,255,0.14),rgba(255,255,255,0.04)_42%,rgba(0,0,0,0.18))]',className)}>{visible?<Image src={src!} alt={alt} fill unoptimized={src!.startsWith('http')} priority={priority} sizes="(max-width:640px) 45vw,(max-width:1024px) 25vw,220px" onError={()=>setFailed(true)} className={cx(variant==='cover'?'object-cover':'object-contain p-5',imageClassName)}/>:<div className="absolute inset-0 flex flex-col items-center justify-center gap-2 text-zinc-600"><Wine className="h-8 w-8" aria-hidden="true"/><span className="text-[10px] uppercase tracking-wider">Sin fotografía</span></div>}</div>}
