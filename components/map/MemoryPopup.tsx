import { CalendarDays, Eye, MapPin, Users } from 'lucide-react'; import type { ProfileMemory } from '@/types/profile'; import { MapOwner } from './MapOwner';
export function MemoryPopup({ memory }: { memory: ProfileMemory }) { return <article className="w-64 space-y-3 bg-zinc-950 p-3 text-zinc-100"><div><span className="text-[10px] font-semibold uppercase tracking-wider text-rose-300">{memory.category || 'Memoria'}</span><h3 className="mt-1 text-base font-semibold text-white">{memory.title}</h3>{memory.description && <p className="mt-2 line-clamp-3 text-xs leading-5 text-zinc-400">{memory.description}</p>}</div><div className="space-y-1.5 text-xs text-zinc-500">{memory.place && <p className="flex gap-1.5"><MapPin className="h-3.5 w-3.5" />{memory.place}</p>}{(memory.date || memory.createdAt) && <p className="flex gap-1.5"><CalendarDays className="h-3.5 w-3.5" />{memory.date || memory.createdAt}</p>}<p className="flex gap-1.5"><Eye className="h-3.5 w-3.5" />{memory.visibility}</p>{memory.participantNames.length > 0 && <p className="flex gap-1.5"><Users className="h-3.5 w-3.5" />{memory.participantNames.join(', ')}</p>}</div><MapOwner id={memory.userId} name={memory.ownerName} avatarUrl={memory.ownerAvatarUrl} /></article>; }
