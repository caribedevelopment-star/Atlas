import { Route } from 'lucide-react'; import type { ProfileStatistics } from '@/types/profile';
export function TravelStats({ stats }: { stats: ProfileStatistics }) { return <section aria-labelledby="travel-title" className="rounded-3xl border border-white/10 bg-zinc-900/50 p-5"><h2 id="travel-title" className="flex items-center gap-2 text-lg font-semibold text-white"><Route className="h-5 w-5 text-emerald-300" />Viajes</h2><dl className="mt-5 grid grid-cols-2 gap-4"><Item label="Viajes" value={String(stats.trips)} /><Item label="Restaurantes" value={String(stats.restaurants)} /><Item label="Países" value={String(stats.countriesVisited)} /><Item label="Distancia" value={stats.travelDistanceKm === null ? 'No disponible' : `${stats.travelDistanceKm.toLocaleString('es')} km`} /></dl>{stats.travelDistanceKm === null && <p className="mt-4 text-xs leading-5 text-zinc-600">La distancia solo se calcula cuando existen al menos dos coordenadas o puntos de ruta válidos.</p>}</section>; }
function Item({ label, value }: { label: string; value: string }) { return <div><dt className="text-xs text-zinc-500">{label}</dt><dd className="mt-1 font-semibold text-white">{value}</dd></div>; }
