import { BookOpen, Calendar, Wine } from 'lucide-react';
import type { ProfileTimelineItem } from '@/types/profile';
import { ProfileEmptyState } from './ProfileEmptyState';
export function ProfileTimeline({ items }: { items: ProfileTimelineItem[] }) { if (!items.length) return <ProfileEmptyState title="Sin actividad visible" description="La actividad aparecerá aquí cuando haya registros disponibles." />; return <section aria-labelledby="timeline-title"><h2 id="timeline-title" className="text-lg font-semibold text-white">Línea de vida</h2><ol className="mt-4 space-y-2">{items.map((item) => { const Icon = item.kind === 'wine' ? Wine : item.kind === 'library' ? BookOpen : Calendar; return <li key={item.id} className="flex gap-3 rounded-2xl border border-white/10 bg-zinc-900/50 p-4"><Icon className="mt-0.5 h-4 w-4 shrink-0 text-rose-300" /><div className="min-w-0"><p className="truncate text-sm font-medium text-white">{item.title}</p>{item.subtitle && <p className="truncate text-xs text-zinc-500">{item.subtitle}</p>}</div>{item.date && <time className="ml-auto shrink-0 text-xs text-zinc-600">{format(item.date)}</time>}</li>; })}</ol></section>; }
function format(value: string) { const date = new Date(value); return Number.isNaN(date.getTime()) ? value : new Intl.DateTimeFormat('es', { dateStyle: 'medium' }).format(date); }
