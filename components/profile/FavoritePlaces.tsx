import { MapPin } from 'lucide-react'; import type { ProfilePlace } from '@/types/profile'; import { ProfileEmptyState } from './ProfileEmptyState';
export function FavoritePlaces({ places }: { places: ProfilePlace[] }) { return <section aria-labelledby="places-title"><h2 id="places-title" className="text-lg font-semibold text-white">Lugares favoritos</h2>{places.length ? <ol className="mt-4 grid gap-2 sm:grid-cols-2">{places.map((place, index) => <li key={place.name} className="flex items-center gap-3 rounded-2xl border border-white/10 bg-zinc-900/50 p-4"><span className="text-xs text-zinc-600">{String(index + 1).padStart(2, '0')}</span><span className="text-sm text-white">{place.name}</span><span className="ml-auto text-xs text-zinc-500">{place.count} visitas</span></li>)}</ol> : <div className="mt-4"><ProfileEmptyState title="Sin lugares disponibles" description="Se muestran únicamente ubicaciones procedentes de memorias visibles." icon={MapPin} /></div>}</section>; }
