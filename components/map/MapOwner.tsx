import Image from 'next/image'; import Link from 'next/link';
export function MapOwner({ id, name, avatarUrl }: { id?: string; name?: string; avatarUrl?: string }) { if (!name) return null; const body = <><span className="relative h-7 w-7 overflow-hidden rounded-lg bg-zinc-800 text-center text-[10px] font-semibold leading-7 text-white">{avatarUrl ? <Image src={avatarUrl} alt="" fill unoptimized={avatarUrl.startsWith('http')} className="object-cover" /> : name.slice(0, 2).toUpperCase()}</span><span className="truncate text-xs text-zinc-400">{name}</span></>; return id ? <Link href={`/profile/${id}`} className="flex items-center gap-2 rounded-lg focus:outline-none focus-visible:ring-2 focus-visible:ring-rose-400">{body}</Link> : <div className="flex items-center gap-2">{body}</div>; }
