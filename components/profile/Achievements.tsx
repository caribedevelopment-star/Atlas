import { Award } from 'lucide-react'; import type { ProfileAchievement } from '@/types/profile'; import { ProfileEmptyState } from './ProfileEmptyState';
export function Achievements({ achievements }: { achievements: ProfileAchievement[] }) { return <section aria-labelledby="achievements-title"><h2 id="achievements-title" className="text-lg font-semibold text-white">Logros</h2>{achievements.length ? <ul className="mt-4 grid gap-3 sm:grid-cols-2">{achievements.map((item) => <li key={item.id} className="rounded-2xl border border-amber-400/15 bg-amber-400/5 p-4"><Award className="h-5 w-5 text-amber-300" /><p className="mt-3 font-medium text-white">{item.title}</p><p className="mt-1 text-xs text-zinc-500">{item.description}</p></li>)}</ul> : <div className="mt-4"><ProfileEmptyState title="Aún no hay logros" description="Los logros se generan solo a partir de actividad real." icon={Award} /></div>}</section>; }
