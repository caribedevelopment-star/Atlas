'use client';
import { useEffect,useState } from 'react'; import { getTripEditorOptions,saveTrip } from '@/lib/trips/repository'; import type { TripInput } from '@/types/trip';
export function useTripEditor(initial:TripInput){const [value,setValue]=useState(initial),[options,setOptions]=useState<Awaited<ReturnType<typeof getTripEditorOptions>>|null>(null),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState<string|null>(null);useEffect(()=>{getTripEditorOptions().then(setOptions).catch((cause)=>setError(cause instanceof Error?cause.message:'No se pudieron cargar las opciones.')).finally(()=>setLoading(false))},[]);const save=async()=>{setSaving(true);setError(null);try{return await saveTrip(value)}catch(cause){setError(cause instanceof Error?cause.message:'No se pudo guardar el viaje.');throw cause}finally{setSaving(false)}};return{value,setValue,options,loading,saving,error,save};}
