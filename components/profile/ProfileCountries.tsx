import { Globe2 } from 'lucide-react'; import type { ProfilePlace } from '@/types/profile'; import { ProfileEmptyState } from './ProfileEmptyState';
export function ProfileCountries({ countries }: { countries: ProfilePlace[] }) { return <PlaceList title="Países visitados" values={countries} empty="No hay países verificables en las memorias visibles." />; }
export function PlaceList({ title, values, empty }: { title: string; values: ProfilePlace[]; empty: string }) { return <section><h2 className="text-lg font-semibold text-white">{title}</h2>{values.length ? <ul className="mt-4 flex flex-wrap gap-2">{values.map((item) => <li key={item.name} className="rounded-full border border-white/10 bg-white/5 px-3 py-2 text-sm text-zinc-300">{item.name} <span className="text-zinc-600">· {item.count}</span></li>)}</ul> : <div className="mt-4"><ProfileEmptyState title="Sin datos" description={empty} icon={Globe2} /></div>}</section>; }
