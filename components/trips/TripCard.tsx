import Image from 'next/image';import Link from 'next/link';import {CalendarDays,MapPin,Route} from 'lucide-react';import type{AtlasTrip}from '@/types/trip';
export function TripCard({trip}:{trip:AtlasTrip}){return <Link href={`/trips/${trip.id}`} className="group overflow-hidden rounded-3xl border border-white/10 bg-zinc-900/60 focus:outline-none focus-visible:ring-2 focus-visible:ring-rose-400"><div className="relative h-44 bg-zinc-800">{trip.coverImageUrl?<Image src={trip.coverImageUrl} alt="" fill unoptimized={trip.coverImageUrl.startsWith('http')} className="object-cover transition group-hover:scale-105"/>:<div className="flex h-full items-center justify-center"><Route className="h-10 w-10 text-zinc-600"/></div>}</div><div className="p-5"><p className="text-xs uppercase tracking-wider text-rose-300">{trip.visibility}</p><h2 className="mt-1 text-lg font-semibold text-white">{trip.title}</h2><div className="mt-3 flex flex-wrap gap-3 text-xs text-zinc-500"><span className="flex gap-1"><CalendarDays className="h-3.5 w-3.5"/>{trip.startDate} — {trip.endDate}</span><span className="flex gap-1"><MapPin className="h-3.5 w-3.5"/>{trip.stops.length} paradas</span>{trip.distanceKm!==null&&<span>{trip.distanceKm} km</span>}</div></div></Link>}
