import { BookOpen, Globe2, Heart, Map, MapPin, MessageSquare, Plane, Store, Users, Wine } from 'lucide-react';
import type { ProfileStatistics } from '@/types/profile';
export function ProfileStats({ stats }: { stats: ProfileStatistics }) { const values = [[MessageSquare, stats.memories, 'Memorias'],[Wine,stats.wines,'Vinos'],[Heart,stats.favoriteWines,'Favoritos'],[Globe2,stats.countriesVisited,'Países'],[MapPin,stats.citiesVisited,'Ciudades'],[Plane,stats.trips,'Viajes'],[Store,stats.restaurants,'Restaurantes'],[BookOpen,stats.libraryItems,'Biblioteca'],[Users,stats.friends,'Amigos'],[Map,stats.publicContributions,'Públicas']] as const; return <section aria-labelledby="profile-stats-title"><h2 id="profile-stats-title" className="sr-only">Resumen</h2><div className="grid grid-cols-2 gap-3 sm:grid-cols-5">{values.map(([Icon,value,label]) => <div key={label} className="rounded-2xl border border-white/10 bg-zinc-900/60 p-4"><Icon className="h-4 w-4 text-rose-300" aria-hidden="true" /><strong className="mt-3 block text-2xl text-white">{value}</strong><span className="text-xs text-zinc-500">{label}</span></div>)}</div></section>; }
