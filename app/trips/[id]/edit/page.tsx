'use client';import{TripEditor,TripError,TripLoading}from'@/components/trips';import{useTrips}from'@/hooks/use-trips';export default function EditTripPage({params}:{params:{id:string}}){const state=useTrips(params.id);if(state.loading)return <TripLoading/>;if(state.error||!state.trip)return <TripError message={state.error||'Viaje no encontrado'} retry={()=>void state.refresh()}/>;return <TripEditor trip={state.trip}/>}
