import {GripVertical,MapPin}from'lucide-react';import type{TripStop}from'@/types/trip';
export function TripStopList({stops}:{stops:TripStop[]}){return <ol className="space-y-2">{stops.map((stop,index)=><li key={stop.id} className="flex items-center gap-3 rounded-2xl border border-white/10 bg-zinc-900/50 p-4"><GripVertical className="h-4 w-4 text-zinc-600"/><span className="flex h-7 w-7 items-center justify-center rounded-full bg-rose-500/15 text-xs text-rose-300">{index+1}</span><div><p className="text-sm font-medium text-white">{stop.title}</p>{(stop.city||stop.country)&&<p className="mt-1 flex gap-1 text-xs text-zinc-500"><MapPin className="h-3 w-3"/>{[stop.city,stop.country].filter(Boolean).join(', ')}</p>}</div></li>)}</ol>}
